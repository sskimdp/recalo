// Сюда вставьте настройки вашего проекта Firebase (см. docs/setup-sync.md).
// Пока значения пустые, приложение работает как раньше: только на этом устройстве.
// Эти ключи не секретные: доступ к данным закрывают правила Firestore, а не они.
export const firebaseConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  appId: '',
};
